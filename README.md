#DCM

DCM (Direction Cosine Matrix) is generally used to represent the attitude of satellites because it's very useful for representing rotation in three dimensions. This code can transform coordinates and attitudes using DCMs.

#Inputs



#Outputs


